#include <bits/stdc++.h>
using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int t;
	cin >> t;

	while (t--)
	{
		cout << 0 << "\n";
		fflush(stdout);

		long long response1;
		cin >> response1;

		cout << 0 << "\n";
		fflush(stdout);

		long long response2;
		cin >> response2;

		cout << "!" << "\n";
		fflush(stdout);

		long long m;
		cin >> m;

		cout << 0 << "\n";
		fflush(stdout);
	}

	return 0;
}
